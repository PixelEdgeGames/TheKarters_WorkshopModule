using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PTK_SuspensionWheelPivot : MonoBehaviour
{
    public Transform innerSideWheelPivotTransform;
    public Transform outerSideWheelPivotTransform;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
